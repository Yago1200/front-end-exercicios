const mult = function (x, y) {
  return x * y;
};

const adic = function (x, y) {
  return x + y;
};

const subt = function (x, y) {
  return x - y;
};

const divi = function (x, y) {
  return x / y;
};

console.log(mult(3, 7));
console.log(adic(3, 7));
console.log(subt(3, 7));
console.log(divi(3, 7));

console.log('✅')